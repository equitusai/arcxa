{{- if .Values.graphica.shards.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: arcxa-shard
  namespace: {{ .Release.namespace }}
spec:
  ports:
  - port: 9100
    name: data
  clusterIP: None
  selector:
    app: graphica
    component: shard
{{- end }}
