{{- if .Values.graphica.coordinator.enabled -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: arcxa-coordinator
  namespace: {{ .Release.namespace }}
spec:
  serviceName: arcxa-coordinator
  replicas: {{ .Values.graphica.coordinator.replicas }}
  selector:
    matchLabels:
      app: graphica
      component: coordinator
  template:
    metadata:
      labels:
        app: graphica
        component: coordinator
    spec:
      serviceAccountName: {{ .Values.graphica.serviceAccountName }}
      serviceAccount: {{ .Values.graphica.serviceAccountName }}
      imagePullSecrets:
      - name: arcxa-pull
      containers:
      - name: arcxa-coordinator
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        image: {{ .Values.graphica.coordinator.imageTag }}
        volumeMounts:
          - name: arcxa-coordinator-metadata
            mountPath: /data
        args:
          - ./arcxa-coordinator
        envFrom:
        - configMapRef:
            name: arcxa-coordinator-configmap
        - secretRef:
            name: arcxa-initial-admin
        # livenessProbe:
        #   httpGet:
        #     path: /health/liveness
        #     port: 8080
        #   initialDelaySeconds: 300
        #   periodSeconds: 120
        #   timeoutSeconds: 10
        # startupProbe:
        #   httpGet:
        #     path: /health/readiness
        #     port: 8080
        #   initialDelaySeconds: 10
        #   periodSeconds: 5
        #   timeoutSeconds: 10
        env:
        - name: RUST_LOG
          value: info
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      volumes:
        - name: arcxa-coordinator-metadata
          persistentVolumeClaim:
            claimName: arcxa-coordinator-metadata
  volumeClaimTemplates:
    - metadata:
        name: arcxa-coordinator-metadata
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.graphica.coordinator.storage.size }}
        storageClassName: {{ .Values.global.storageClassName }}
        volumeMode: Filesystem
{{- end }}
