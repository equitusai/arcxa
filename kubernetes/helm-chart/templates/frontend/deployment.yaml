{{- if .Values.graphica.frontend.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: arcxa-frontend
  namespace: {{ .Release.namespace }}
spec:
  replicas: {{ .Values.graphica.frontend.replicas }}
  selector:
    matchLabels:
      app: graphica
      component: frontend
  template:
    metadata:
      labels:
        app: graphica
        component: frontend
    spec:
      serviceAccountName: {{ .Values.graphica.serviceAccountName }}
      imagePullSecrets:
      - name: arcxa-pull
      containers:
      - name: arcxa-frontend
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        image: {{ .Values.graphica.frontend.imageTag }}
        env:
        - name: VITE_API_URL
          value: 'http://arcxa-coordinator:8080/api/v1'
        securityContext:
          runAsUser: 1000
{{- end }}
